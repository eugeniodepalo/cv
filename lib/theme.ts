interface Theme {
  bgColor: string
  primaryColor: string
  linkColor: string
  activeColor: string
  errorColor: string
}

const theme: Theme = {
  bgColor: '#f5f5f5',
  primaryColor: '#333',
  linkColor: '#fff',
  activeColor: '#FFD74F',
  errorColor: '#FF665A'
}

export default theme
